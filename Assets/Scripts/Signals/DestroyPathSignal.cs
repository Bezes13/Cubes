using Supyrb;

namespace Signals
{
    /// <summary>
    /// Signal which gets called when all paths should get destroyed except the main path
    /// </summary>
    public class DestroyPathSignal : Signal
    {
        
    }
}